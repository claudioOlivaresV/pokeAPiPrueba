import { ExtractNumberPipe } from './extract-number.pipe';

describe('ExtractNumberPipe', () => {
  it('create an instance', () => {
    const pipe = new ExtractNumberPipe();
    expect(pipe).toBeTruthy();
  });
});
